name: Environment vairiables

on: push

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
    steps:
      - run: echo "${BRANCH}"
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.BRANCH }}
